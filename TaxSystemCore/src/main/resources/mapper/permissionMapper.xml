<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ey.tax.core.dao.PermissionDAO">

    <select id="findAll" resultType="com.ey.tax.entity.SysPermission">
        select * from t_sys_permission;
    </select>

    <select id="findByUserId" parameterType="long" resultType="com.ey.tax.entity.SysPermission">
        select p.*
          from t_sys_user u
          left join t_sys_user_role_ref sru on u.id = sru.user_id
          left join t_sys_role r on sru.role_id = r.id
          left join t_sys_role_permission_ref spr on spr.role_id = r.id
          left join t_sys_permission p on p.id = spr.permission_id
          where u.id=#{userId}
    </select>

</mapper>